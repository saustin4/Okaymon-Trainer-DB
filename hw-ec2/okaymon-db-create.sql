/*
 * StacyAustin
 * ITEC325
 * Fall2015
 * SQL script for "Okaymon" database creation
 */

CREATE TABLE okaymon 
  ( 
     name    VARCHAR(100), 
     species VARCHAR(100), 
     pounds  VARCHAR(4), 
     units   VARCHAR(4), 
     flavor  VARCHAR(200), 
     energy  VARCHAR(50), 
     clover  VARCHAR(50), 
     candle  VARCHAR(50), 
     puddle  VARCHAR(50), 
     spark   VARCHAR(50), 
     thinkin VARCHAR(50) 
  ); 